{
  "name": "SendIT",
  "description": "Api for accessing parcel delivery service.",
  "image": "heroku/python",
  "repository": "https://github.com/ElMonstro/SendIT-API",
  "keywords": ["python", "python" ],
  "addons": [ "heroku-postgresql" ],
  "env": {
    "SECRET_KEY": {
      "description": "The secret key for the Django application.",
      "generator": "secret"
    }
  },
  "environments": {
    "test": {
      "scripts": {
        "test": "pytest --cov"
      }
    }
  }
}
